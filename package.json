{
  "name": "parserizer",
  "version": "0.0.0",
  "description": "A simple way to parse data.",
  "main": "./lib/cjs/index.js",
  "module": "./lib/esm/index.js",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "test": "jest",
    "build": "npm run clear && npm run build:node && npm run build:browser",
    "build:browser": "node esbuild",
    "build:node": "tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
    "clear": "rm -rf lib",
    "prepublishOnly": "npm run build"
  },
  "keywords": [],
  "author": "FriquetLuca",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FriquetLuca/parserizer.git"
  },
  "homepage": "https://github.com/FriquetLuca/parserizer",
  "devDependencies": {
    "@babel/preset-typescript": "^7.22.5",
    "@types/jest": "^29.5.2",
    "esbuild": "^0.18.11",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.1",
    "typescript": "^4.9.5"
  }
}
